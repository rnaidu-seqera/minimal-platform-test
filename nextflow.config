manifest {
    name        = 'minimal-platform-test'
    description = 'Minimal pipeline for Seqera Platform testing'
    version     = '1.0.0'
    nextflowVersion = '>=24.11.0'
}

params {
    greeting = "Hello"
    name     = "World"
    outdir   = "s3://scidev-playground-eu-west-2/scratch/testrun"
}

process {
    publishDir = [
        path: { "${params.outdir}/${env('TOWER_WORKFLOW_ID')}/${task.process}/" },
        mode: 'copy'
    ]
}

{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/seqeralabs/minimal-platform-test/main/nextflow_schema.json",
    "title": "minimal-platform-test pipeline parameters",
    "description": "Minimal pipeline for Seqera Platform testing — FD-7482",
    "type": "object",
    "$defs": {
        "main_options": {
            "title": "Main options",
            "type": "object",
            "description": "Pipeline parameters",
            "properties": {
                "call_min_reads": {
                    "type": "string",
                    "default": "1",
                    "description": "Minimum reads threshold. Accepts a single integer or a comma-separated list of up to three integers (e.g. '1' or '1,1,1').",
                    "pattern": "^[0-9]+(,[0-9]+){0,2}$",
                    "errorMessage": "call_min_reads must be a string: one to three comma-separated integers (e.g. '1' or '1,1,1')"
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/main_options"
        }
    ]
}

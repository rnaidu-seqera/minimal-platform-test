{
    "$id": "https://raw.githubusercontent.com/minimal-platform-test/main/nextflow_schema.json",
    "title": "minimal-platform-test pipeline parameters",
    "description": "Minimal pipeline for Seqera Platform testing",
    "type": "object",
    "definitions": {
        "main_options": {
            "title": "Main options",
            "type": "object",
            "description": "Main pipeline parameters",
            "properties": {
                "greeting": {
                    "type": "string",
                    "default": "Hi",
                    "description": "Greeting word"
                },
                "name": {
                    "type": "string",
                    "default": "Universe",
                    "description": "Name to greet"
                },
                "results_prefix": {
                    "type": "string",
                    "default": "bioinformatics-results",
                    "description": "Prefix for results output (hidden: false set explicitly — FD-7451)",
                    "hidden": false
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/main_options"
        }
    ]
}
